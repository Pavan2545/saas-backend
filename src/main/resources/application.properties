# Database
spring.datasource.url=jdbc:mysql://localhost:3306/saasdb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=root

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT
jwt.secret=change_this_secret_to_secure_value
jwt.expiration-ms=86400000

# Server
server.port=8080
